import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  // App Store Connectでアプリを作成する前なら変更できます。正式登録前に最終確認してください。
  appId: "com.jugglerarata.tsumeshogi",
  appName: "Tsume Shogi",
  webDir: "public",
};

export default config;
